import { AgendapipePipe } from './agendapipe.pipe';

describe('AgendapipePipe', () => {
  it('create an instance', () => {
    const pipe = new AgendapipePipe();
    expect(pipe).toBeTruthy();
  });
});
